{
  "name": "catology-api",
  "main": "server.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "start": "npm run build && node lib/index.js"
  },
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
